<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Add Student</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-light">

<div class="container mt-5">

    <h3 class="mb-4">Add Student</h3>

    <!-- ✅ FIXED URL -->
    <form th:action="@{/admin/save-student}" th:object="${student}" method="post">

        <input type="hidden" th:field="*{id}">

        <div class="mb-2">
            <label>Name</label>
            <input th:field="*{name}" class="form-control" required>
        </div>

        <div class="mb-2">
            <label>Email</label>
            <input th:field="*{email}" class="form-control" required>
        </div>

        <div class="mb-2">
            <label>Password</label>
            <input th:field="*{password}" class="form-control" required>
        </div>

        <div class="mb-2">
            <label>Course</label>
            <input th:field="*{course}" class="form-control">
        </div>

        <div class="mb-2">
            <label>Grade</label>
            <input th:field="*{grade}" class="form-control">
        </div>

        <div class="mb-2">
            <label>Attendance</label>
            <input th:field="*{attendance}" class="form-control">
        </div>

        <div class="mb-2">
            <label>Progress</label>
            <input th:field="*{progress}" class="form-control">
        </div>

        <div class="mb-3">
            <label>Fees</label>
            <input th:field="*{feesPaid}" class="form-control">
        </div>

        <button class="btn btn-success">Save</button>

        <!-- ✅ FIXED BACK BUTTON -->
        <a th:href="@{/admin/dashboard}" class="btn btn-secondary">Back</a>

    </form>

</div>
</body>
</html>
