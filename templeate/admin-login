<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>AdminLogin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<div class="container">
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
      <a class="navbar-brand" href="#"> TrackScore</a>
    </div>
  </nav>
</div>





<body class="bg-light">
<div class="container mt-5">
    <div class="card p-4 shadow">
        <h3 class="text-center">Admin Login</h3>
        <form th:action="@{/admin/login}" method="post">
        
            <div class="mb-3">
                <input type="text" name="username" class="form-control" placeholder="Username" required>
            </div>

            <div class="mb-3">
                <input type="password" name="password" class="form-control" placeholder="Password" required>
            </div>

            <button class="btn btn-primary w-100">Login</button>
        </form>

        <p class="text-danger mt-2" th:text="${error}"></p>
    </div>
</div>


<div class="container mt-5">
    <div class="card p-4 shadow">

        <p class ="btn btn-danger mt-3">New Students click register </p>
        <div>
        <a th:href="@{/admin/add-student}" class="btn btn-danger mt-3"> Register </a>
     
        </div>
       
        <h3 class="text-center"> Students Login</h3>
        <form th:action="@{/student/login}" method="post">
        
            <div class="mb-3">
                <input type="email" name="email" class="form-control" placeholder="Email" required>
            </div>

            <div class="mb-3">
                <input type="password" name="password" class="form-control" placeholder="Password" required>
            </div>

            <button class="btn btn-primary w-100">Login</button>
        </form>
        
        <p class="text-danger mt-2" th:text="${error}"></p>
    </div>
</div>









</body>
</html>
